name = "payizi-software"
compatibility_date = "2024-01-01"

# Pages configuration - deploy from root directory
pages_build_output_dir = "."

# No build process needed for static site
# Routes configuration handled by Cloudflare Pages automatically

name = "payizi-software"
compatibility_date = "2024-01-01"

# Pages configuration
pages_build_output_dir = "."

[build]
command = ""
cwd = ""
watch_dirs = []

# Environment variables (if needed)
[vars]

# Routes configuration (handled by Cloudflare Pages automatically)
